scalar DateTime
scalar Cursor

interface Node {
	id: ID!
}

enum ConversationKind {
	PRIVATE
	GROUP
}

enum IdentityKind {
	GEBNA_USER
	EXTERNAL_EMAIL
}

"""
Conversation placement for the viewer.
Purely organizational (no gating).
"""
enum Mailbox {
	IMPORTANT
	TRASH
}

enum ParticipantRole {
	MEMBER
	ADMIN
}

enum ParticipantState {
	ACTIVE
	LEFT
}

enum Transport {
	EMAIL
	GEBNA_DM
}

enum DeliveryStatus {
	QUEUED
	SENT
	DELIVERED
	READ
	FAILED
}

type Identity implements Node {
	id: ID!

	"""
	Canonical address-like identifier.
	Examples:
	- "alice@gebna.net"
	- "bob@gmail.com"
	"""
	address: String!

	kind: IdentityKind!
	name: String
	avatar: String!

	"""
	Viewer-scoped relationship metadata (contacts).
	Always resolved.
	"""
	relationshipToViewer: IdentityRelationship
}

type IdentityRelationship implements Node {
	"""
	Stable viewer-scoped relationship id.
	Virtual or persisted depending on implementation.
	"""
	id: ID!

	"""
	Whether the viewer saved this identity as a contact.
	"""
	isContact: Boolean!

	"""
	Viewer-scoped display metadata.
	Empty string allowed when not customized.
	"""
	displayName: String
	avatarUrl: String
}

type Conversation implements Node {
	id: ID!
	kind: ConversationKind!
	title: String
	participants: [ConversationParticipant!]!
	lastMessage: Message!
	updatedAt: DateTime!
	viewerState: ConversationViewerState!
	messages(last: Int!, before: Cursor): MessageConnection!
}

type ConversationParticipant {
	identity: Identity!
	role: ParticipantRole!
	state: ParticipantState!
	joinedAt: DateTime!

	"""
	Last message read by this participant.
	Use a sentinel ID if none.
	"""
	lastReadMessageId: ID
}

type ConversationViewerState {
	mailbox: Mailbox!
	unreadCount: Int!
}

type Message implements Node {
	id: ID!

	conversationId: ID!

	sender: Identity!

	bodyText: String
	bodyHTML: String
	bodyMD: String
	createdAt: DateTime!

	delivery: [DeliveryReceipt!]!
}

type DeliveryReceipt {
	recipient: Identity!
	status: DeliveryStatus!
	latestStatusChangeAt: DateTime!
	error: String
	transport: Transport!
}

type PageInfo {
	hasNextPage: Boolean!
	endCursor: Cursor
}

type ConversationConnection {
	edges: [ConversationEdge!]!
	pageInfo: PageInfo!
}

type ConversationEdge {
	cursor: Cursor!
	node: Conversation!
}

type MessageConnection {
	edges: [MessageEdge!]!
	pageInfo: PageInfo!
}

type MessageEdge {
	cursor: Cursor!
	node: Message!
}

type IdentityConnection {
	edges: [IdentityEdge!]!
	pageInfo: PageInfo!
}

type IdentityEdge {
	cursor: Cursor!
	node: Identity!
}

type Viewer implements Node {
	id: ID!
	username: String!
	name: String!
	avatar: String!

	identity: Identity!

	conversations(mailbox: Mailbox!, first: Int!, after: Cursor): ConversationConnection!

	"""
	List of saved contacts.
	"""
	contacts(query: String, first: Int!, after: Cursor): IdentityConnection!
}

type Query {
	viewer: Viewer
	node(id: ID!): Node
}

input UpsertConversationInput {
	kind: ConversationKind!

	"""
	For PRIVATE: exactly 1 address (the other participant).
	Viewer is implied.
	For GROUP: N addresses; viewer is implied.
	"""
	participantAddresses: [String!]!

	title: String!
}

type UpsertConversationPayload {
	conversation: Conversation!
	created: Boolean!
}

input SendMessageInput {
	conversationId: ID!
	bodyText: String!
	clientMutationId: String!
}

type SendMessagePayload {
	clientMutationId: String!
	message: Message!
}

type RecipientTransportDecision {
	recipient: Identity!
	chosen: Transport!
	reason: String!
}

input MarkConversationReadInput {
	conversationId: ID!
	lastReadMessageId: ID!
}

type MarkConversationReadPayload {
	conversation: Conversation!
}

input SetContactStatusInput {
	identityId: ID!
	isContact: Boolean!
	displayName: String
	avatarUrl: String
}

input MoveConversationInput {
	conversationId: ID!
	mailbox: Mailbox!
}

input AddConversationParticipantsInput {
	conversationId: ID!
	participantAddresses: [String!]!
}

type Mutation {
	upsertConversation(input: UpsertConversationInput!): UpsertConversationPayload!
	addConversationParticipants(input: AddConversationParticipantsInput!): Conversation
	sendMessage(input: SendMessageInput!): SendMessagePayload!
	markConversationRead(input: MarkConversationReadInput!): MarkConversationReadPayload!

	setContactStatus(input: SetContactStatusInput!): Identity
	moveConversation(input: MoveConversationInput!): Conversation
}

type Subscription {
	messageAdded(conversationId: ID!): Message!
	conversationUpdated: Conversation!
	deliveryUpdated(messageId: ID!): [DeliveryReceipt!]!
}

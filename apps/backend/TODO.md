## Compliance
- [ ] The session document advertises empty `accountCapabilities` entries for Core/Mail/Submission/Vacation per account instead of the RFC-required capability-specific objects (apps/backend/src/jmap.routes.ts:98-134).
- [ ] All of the simpler `/get` handlers ignore the `properties` argument and always emit every field (e.g. Mailbox/get, Identity/get, EmailSubmission/get, PushSubscription/get, Thread/get), which violates RFC 8620’s requirement to only return requested properties (apps/backend/src/lib/jmap/method-handlers/mailbox-get.ts:1-120, identity-get.ts:1-120, email-submission-get.ts:1-120, push-subscription-get.ts:1-100, thread-get.ts:1-100).
- [ ] Every `/changes` handler treats `sinceState` as optional by defaulting it to `"0"` instead of returning `invalidArguments` when it is missing, even though RFC 8620 marks `sinceState` as mandatory (apps/backend/src/lib/jmap/method-handlers/email-changes.ts:17-40, thread-changes.ts:17-40, mailbox-changes.ts:17-40, identity-changes.ts:17-40, email-submission-changes.ts:17-36).
